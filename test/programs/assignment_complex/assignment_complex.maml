// Tests that we can evaluate complex expressions during assignment
fn main() int {
    mut a := 5
    mut b := 10
    
    // Reassign 'a' to the result of a math expression involving itself
    a = (a * 2) + b
    
    return a // Should exit with status 20
}