// Callee takes ownership. When consume() exits, its popScope will free the slice.
fn consume(own s []int) int {
    return s[2]
}

fn main() int {
    arr := [5, 10, 15]
    s := arr[:]
    
    // Ownership leaves this frame.
    res := consume(own s)
    
    return res
}