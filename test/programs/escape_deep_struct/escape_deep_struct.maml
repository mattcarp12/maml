type Box = { id int, data []int }

fn make_box(id int) Box {
    arr := [10, 20, 30]
    s := arr[:]
    // The struct is stack allocated, but the slice backing array escapes!
    return Box{id: id, data: s}
}

fn main() int {
    b := make_box(42)
    // 20 + 42 = 62
    return b.data[1] + b.id
}